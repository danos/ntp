#!/bin/sh

rm -f /etc/cron.daily/ntp /etc/cron.weekly/ntp

#DEBHELPER#

# Purge old debconf setting
if test -r /usr/share/debconf/confmodule ; then
	. /usr/share/debconf/confmodule
	db_purge shared/ntp/servers
	db_go
fi

exit 0
