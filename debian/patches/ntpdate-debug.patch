Index: ntp-4.2.4p0+dfsg/ntpdate/ntpdate.c
===================================================================
--- ntp-4.2.4p0+dfsg.orig/ntpdate/ntpdate.c	2007-02-22 12:02:08.000000000 +0100
+++ ntp-4.2.4p0+dfsg/ntpdate/ntpdate.c	2007-04-10 10:48:06.000000000 +0200
@@ -1768,7 +1768,7 @@
 	/*
 	 * bind the socket to the NTP port
 	 */
-	if (!debug && !simple_query && !unpriv_port) {
+	if (!simple_query && !unpriv_port) {
 		if (bind(fd[nbsock], res->ai_addr, SOCKLEN(res->ai_addr)) < 0) {
 #ifndef SYS_WINNT
 			if (errno == EADDRINUSE)
@@ -2047,8 +2047,10 @@
 		adjtv.tv_usec = -adjtv.tv_usec;
 	}
 
-	if (adjtv.tv_usec != 0 && !debug) {
-		if (adjtime(&adjtv, &oadjtv) < 0) {
+	if (adjtv.tv_usec != 0) {
+		if (debug) {
+			msyslog(LOG_DEBUG, "Debug mode --not changing the system date");
+		} else if (adjtime(&adjtv, &oadjtv) < 0) {
 			msyslog(LOG_ERR, "Can't adjust the time of day: %m");
 			return 0;
 		}
@@ -2074,7 +2076,6 @@
 	int isneg;
 	int n;
 
-	if (debug) return 1;
 	/*
 	 * Take the absolute value of the offset
 	 */
@@ -2101,8 +2102,6 @@
 	 * line.
 	 */
 #endif
-	if (debug)
-		return 1;
 #ifdef FORCE_NTPDATE_STEP
 	LFPTOD(ts, dtemp);
 	return step_systime(dtemp);
@@ -2111,8 +2110,6 @@
 	return 1;
 #endif
 #else /* SLEWALWAYS */
-	if (debug)
-		return 1;
 	LFPTOD(ts, dtemp);
 	return step_systime(dtemp);
 #endif	/* SLEWALWAYS */
